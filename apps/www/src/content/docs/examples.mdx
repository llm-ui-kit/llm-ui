---
title: Examples
description: ""
---

import { GithubLinkButton } from "@/components/ui/custom/GithubLinkButton";
import { examplesUrl } from "@/constants/constants";

llm-ui provides example code to get you started.

# Browse the examples

<GithubLinkButton href={examplesUrl} />

# Markdown example

An example of how to use the markdown fallback block.

<GithubLinkButton href={`${examplesUrl}/markdown`} />

# Code block example

An example of how to use the code block with Shiki.

<GithubLinkButton href={`${examplesUrl}/code`} />

# Buttons example

An example of how to use the code block with Shiki.

<GithubLinkButton href={`${examplesUrl}/code`} />

# OpenAI example

An example of how to use llm-ui with OpenAI.

<GithubLinkButton href={`${examplesUrl}/openai`} />

# vercel.ai example

An example of how to use llm-ui with [vercel-ai](https://vercel.com/ai)

<GithubLinkButton href={`${examplesUrl}/vercel-ai/nextjs`} />
