---
title: Buttons block
description: ""
---

import { ExampleSideBySide } from "@/components/examples/ExampleMdx";
import { CodeBlock } from "@/components/ui/custom/CodeBlock";
import { PackageInstall } from "@/components/docs/PackageInstall";
import { buttonPrompt } from "@llm-ui/buttons";

The buttons block allows LLMs to display buttons in the LLM output.

<ExampleSideBySide
  client:load
  example={
    "Buttons example\n<buttons>\n  <button>Button 1</button>\n  <button>Button 2</button>\n</buttons>"
  }
  options={{ delayMultiplier: 0.2 }}

/>

# Installation

<PackageInstall client:load packages={["llm-ui", "@llm-ui/buttons"]} />

# Quick start

Copy and paste [this file](https://github.com/llm-ui-kit/llm-ui/blob/main/examples/nextjs/src/app/examples/buttons/page.tsx) into your project to get started.

# Prompt

<CodeBlock className="whitespace-pre-wrap" code={buttonPrompt} />

You can import this prompt in code:

<CodeBlock
  className="whitespace-pre-wrap"
  codeToHtmlProps={{
    lang: "typescript",
  }}
  code={`import { buttonPrompt } from "@llm-ui/buttons";\n\nconsole.log(buttonPrompt);\n// => "${buttonPrompt.slice(0, 53)}..."`}
/>

# Code block functions

```tsx
const buttonsBlock = {
  findPartialMatch: findPartialButtons(),
  findCompleteMatch: findCompleteButtons(),
  lookBack: buttonsLookBack(),
  component: () => <div>Buttons</div>,
};
```

## `findCompleteButtons`

Finds a [complete buttons block](/docs/llm-output-hook#blocks-object) in a string.

## `findPartialButtons`

Find a [partial buttons block](/docs/llm-output-hook#blocks-object) in a string.

## `buttonsLookBack`

[Look back function](/docs/llm-output-hook#blocks-object) for the buttons block.

# `parseCompleteButtons`

You can use the `parseCompleteButtons` to parse a buttons block. This is helpful for building a custom component for buttons.

```ts
parseCompleteButtons(
  "<buttons><button>Button 1</button><button>Button 2</button></buttons>",
);

// => ['Button 1', 'Button 2']
```
